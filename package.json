{
  "name": "fairshare",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "dev": "turbo run dev --filter=@fairshare/web",
    "dev:web": "npm run dev --workspace @fairshare/web",
    "lint": "turbo run lint",
    "lint:web": "npm run lint --workspace @fairshare/web",
    "typecheck": "turbo run typecheck",
    "typecheck:web": "npm run typecheck --workspace @fairshare/web",
    "db:generate": "npm run db:generate --workspace @fairshare/web",
    "db:migrate": "npm run db:migrate --workspace @fairshare/web",
    "db:push": "npm run db:push --workspace @fairshare/web",
    "db:studio": "npm run db:studio --workspace @fairshare/web"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "npm@11.3.0"
}
